version: "3.9"

services:
  sniper-bot:
    build: .
    restart: unless-stopped
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - DATA_DIR=/app/data
    volumes:
      - ./data:/app/data        # 持久化用户数据
      - ./config:/app/config    # 挂载配置文件（含自选池等非敏感配置）
